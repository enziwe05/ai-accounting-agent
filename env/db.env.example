# Copy this to env/db.env on the server (it is git-ignored).
#
# Invent one long, random password for the MySQL server. The SAME value must go
# in every company's DB_PASS (e.g. env/m26.env). Nothing else uses this file.

MYSQL_ROOT_PASSWORD=change-me-to-a-long-random-password
